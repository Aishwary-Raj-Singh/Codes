results = [588, 665, 216, 113, 642, 4, 836, 114, 851, 492, 819, 237]

for p in range(100,1000):
    for x in range(1,1000):
        for i in range(11):
            if pow(x,1,p) == results[i]:
                print(x,p)
                #if pow(x,2,p) == results[i]:
                    #print(x,p)
                #print(x, p)
                #    if pow(x,i,p) == results[2]:
                    #print(x, p)
                 #       if pow(x,i,p) == results[3]:
                        #print(x, p)
                  #          if pow(x,i,p) == results[4]:
                            #print(x, p)
                   #             if pow(x,i,p) == results[5]:
                                    #print(x, p)
                    #                if pow(x,i,p) == results[6]:
                                    #print(x, p)
                     #                   if pow(x,i,p) == results[7]:
                                        #print(x, p)
                      #                      if pow(x,i,p) == results[8]:
                                            #print(x, p)
                       #                         if pow(x,i,p) == results[9]:
                                                #print(x, p)
                        #                            if pow(x,i,p) == results[10]:
                                                   # print(x, p)
                         #                               if pow(x,i,p) == results[11]:
                          #                                  break;

#print(x, p)
